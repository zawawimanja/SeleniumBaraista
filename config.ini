import configparser

config = configparser.ConfigParser()
config.read('config.ini')

url = config['DEFAULT']['LoginURL']
username = config['CREDENTIALS']['Username']
password = config['CREDENTIALS']['Password']
